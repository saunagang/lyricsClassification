- stable project so anyone can use it out of the box (no gdrive, ready pip installs)
- maybe do a training run on all permutations of the genres to see which are max compatible (too expensive)
-check out which tokens are the most important (doing atm)
-maybe convert to python module, and then use in notebook? (did that )
-tune the learning aspect more (different learning rate, early stopping, hyperparameter?) (too much)